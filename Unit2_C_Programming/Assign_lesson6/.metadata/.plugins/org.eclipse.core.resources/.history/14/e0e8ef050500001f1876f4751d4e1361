/*
 ============================================================================
 Name        : main.c
 Author      : elsafi
 Version     :
 Copyright   : Your copyright notice
 Description : EXs add two distances inch,feet using structures
 ============================================================================
 */

#include <stdio.h>

struct Sdistance{

	float feet;
	float inch;
};

int main(void) {

  struct Sdistance distanc1,distance2,result;

  printf("Enter information of 1st distance:");
  //stores the distance 1
  printf("\r\nEnter the feet:");
  fflush(stdin);
  fflush(stdout);
  scanf("%f",&distanc1.feet);
  //store student roll
  printf("\r\nEnter the inch:");
  fflush(stdin);
  fflush(stdout);
  scanf("%d",&distanc1.inch);

  printf("Enter information of 2nd distance:");
  //stores the distance 1
  printf("\r\nEnter the feet:");
  fflush(stdin);
  fflush(stdout);
  scanf("%f",&distanc1.feet);
  //store student roll
  printf("\r\nEnter the inch:");
  fflush(stdin);
  fflush(stdout);
  scanf("%d",&distanc1.inch);





  //display student information.
  printf("\r\nDisplaying the information");
  printf("\r\nName:%s",S.name);
  printf("\r\nRoll:%d",S.roll);
  printf("\r\nMarks:%.2f",S.marks);



	return 0;
}


